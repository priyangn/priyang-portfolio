# Dependencies
node_modules/

# Environment and secrets (never commit real keys)
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
**/secrets.*

# Note: js/supabase-config.js is committed so static hosts (GitHub Pages, Netlify)
# receive the browser Supabase anon client. Never commit service_role keys; use RLS in Supabase.

# OS / editor noise
.DS_Store
Thumbs.db
*.log
